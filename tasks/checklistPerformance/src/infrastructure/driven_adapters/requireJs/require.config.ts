// require.config.ts
(function(require) {
    require.config({
        baseUrl: './src/infrastructure/driven_adapters',
        paths: {
            'modulo': 'modulo',
        }
    });
})(require);
